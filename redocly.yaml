apis:
  internal@latest:
    root: ./openapi.yaml
  external@latest:
    root: ./openapi.yaml
    decorators:
      remove-x-internal: on
extends:
  - recommended
rules:
  no-unused-components: error
  no-server-example.com: off
